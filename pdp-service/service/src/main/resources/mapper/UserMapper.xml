<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.slow.service.mapper.UserMapper">

    <!-- 插入用户，useGeneratedKeys返回自增id -->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users(username, email, password, created_at, updated_at)
        VALUES(#{username}, #{email}, #{password}, NOW(), NOW())
    </insert>

    <!-- 根据用户名查询 -->
    <select id="findByUsername" resultType="com.slow.pojo.entity.User">
        SELECT id, username, email, student_id, password, real_name, avatar, major, grade_year, created_at, updated_at
        FROM users
        WHERE username = #{username}
    </select>

    <!-- 根据邮箱查询 -->
    <select id="findByEmail" resultType="com.slow.pojo.entity.User">
        SELECT id, username, email, student_id, password, real_name, avatar, major, grade_year, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <!-- 根据用户名或邮箱查询（登录用），LIMIT 1保证最多返回一条 -->
    <select id="findByUsernameOrEmail" resultType="com.slow.pojo.entity.User">
        SELECT id, username, email, student_id, password, real_name, avatar, major, grade_year, created_at, updated_at
        FROM users
        WHERE username = #{username} OR email = #{email}
        LIMIT 1
    </select>

    <!-- 根据ID查询 -->
    <select id="findById" resultType="com.slow.pojo.entity.User">
        SELECT id, username, email, student_id, password, real_name, avatar, major, grade_year, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

</mapper>
