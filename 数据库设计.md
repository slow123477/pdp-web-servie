# 个人发展规划网站 — 数据库设计文档

> 版本：v1.0 | 日期：2026-04-23 | 数据库：MySQL 8.0+

---

## 1. 设计概述

本文档依据《个人发展规划网站需求文档》及前端原型页面，设计了一套完整的数据库表结构。采用 **MySQL 8.0+** 作为数据库，利用 JSON 类型存储配置类、列表类数据，减少不必要的关联表，同时保证扩展性。

### 设计原则

- **单表职责清晰**：每个功能模块对应独立数据表
- **JSON 适度使用**：配置项、分析结果等半结构化数据使用 JSON 存储，避免过度设计
- **统一时间字段**：所有业务表均包含 `created_at` / `updated_at`
- **外键级联**：用户删除时，关联数据由业务层或软删除控制（未设物理外键级联删除）

### 字符集与排序规则

```sql
DEFAULT CHARSET = utf8mb4
COLLATE = utf8mb4_unicode_ci;
```

---

## 2. ER 关系图

```
┌─────────────────┐
│     users       │
│   (用户表)       │
└────────┬────────┘
         │ 1
         │
         ▼ N
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│    courses      │    │  experiences    │    │  achievements   │
│   (课程表)       │    │   (经历表)       │    │   (成就表)       │
└─────────────────┘    └─────────────────┘    └─────────────────┘

┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│     roles       │    │  ai_analysis    │    │  user_settings  │
│   (角色表)       │    │ (AI分析记录表)   │    │  (用户设置表)    │
└─────────────────┘    └─────────────────┘    └─────────────────┘

┌─────────────────┐
│ data_operations │
│(导入导出记录表)  │
└─────────────────┘
```

**关系说明**：`users` 与其余各表为 **一对多(1:N)** 关系；`users` 与 `user_settings` 为 **一对一(1:1)** 关系。

---

## 3. 表结构设计

### 3.1 users — 用户表

| 字段 | 类型 | 约束 | 默认值 | 说明 |
|------|------|------|--------|------|
| id | BIGINT | PK, AUTO_INCREMENT | | 用户ID |
| username | VARCHAR(50) | NOT NULL, UNIQUE | | 用户名 |
| email | VARCHAR(100) | NOT NULL, UNIQUE | | 邮箱 |
| student_id | VARCHAR(50) | UNIQUE | NULL | 学号 |
| password | VARCHAR(255) | NOT NULL | | 加密密码(BCrypt) |
| real_name | VARCHAR(50) | | NULL | 真实姓名 |
| avatar | VARCHAR(255) | | NULL | 头像URL |
| major | VARCHAR(100) | | NULL | 专业 |
| grade_year | TINYINT | | NULL | 年级: 1/2/3/4 |
| created_at | DATETIME | | CURRENT_TIMESTAMP | 注册时间 |
| updated_at | DATETIME | | CURRENT_TIMESTAMP | 更新时间 |

---

### 3.2 courses — 课程表

| 字段 | 类型 | 约束 | 默认值 | 说明 |
|------|------|------|--------|------|
| id | BIGINT | PK, AUTO_INCREMENT | | 课程ID |
| user_id | BIGINT | NOT NULL, FK | | 所属用户 |
| course_name | VARCHAR(200) | NOT NULL | | 课程名称 |
| course_type | VARCHAR(20) | NOT NULL | | 类型: 必修/选修/通识 |
| credits | DECIMAL(3,1) | NOT NULL | | 学分 |
| semester | VARCHAR(20) | NOT NULL | | 学期, 如"2024-2025-1" |
| academic_year | VARCHAR(20) | | NULL | 学年, 如"2024-2025" |
| score | DECIMAL(5,1) | | NULL | 成绩(0-100) |
| grade_point | DECIMAL(3,2) | | NULL | 绩点 |
| created_at | DATETIME | | CURRENT_TIMESTAMP | |
| updated_at | DATETIME | | CURRENT_TIMESTAMP | |

---

### 3.3 experiences — 经历表

| 字段 | 类型 | 约束 | 默认值 | 说明 |
|------|------|------|--------|------|
| id | BIGINT | PK, AUTO_INCREMENT | | |
| user_id | BIGINT | NOT NULL, FK | | 所属用户 |
| title | VARCHAR(200) | NOT NULL | | 经历标题 |
| type | VARCHAR(20) | NOT NULL | | 竞赛/项目/实习 |
| start_date | DATE | NOT NULL | | 开始日期 |
| end_date | DATE | | NULL | 结束日期(null=至今) |
| description | TEXT | | NULL | 详细描述 |
| result | VARCHAR(500) | | NULL | 成果/奖项 |
| attachments | JSON | | NULL | 证明材料 `[{"name":"","url":""}]` |
| created_at | DATETIME | | CURRENT_TIMESTAMP | |
| updated_at | DATETIME | | CURRENT_TIMESTAMP | |

---

### 3.4 achievements — 成就表

| 字段 | 类型 | 约束 | 默认值 | 说明 |
|------|------|------|--------|------|
| id | BIGINT | PK, AUTO_INCREMENT | | |
| user_id | BIGINT | NOT NULL, FK | | 所属用户 |
| name | VARCHAR(200) | NOT NULL | | 成就名称 |
| level | VARCHAR(20) | NOT NULL | | 级别: 国家级/省级/校级/院级 |
| category | VARCHAR(50) | | NULL | 分类: 学术/竞赛/志愿/其他 |
| issuer | VARCHAR(200) | | NULL | 颁发机构 |
| award_date | DATE | | NULL | 获得日期 |
| description | TEXT | | NULL | 描述 |
| certificate_url | VARCHAR(255) | | NULL | 证书图片URL |
| created_at | DATETIME | | CURRENT_TIMESTAMP | |
| updated_at | DATETIME | | CURRENT_TIMESTAMP | |

---

### 3.5 roles — 角色表

| 字段 | 类型 | 约束 | 默认值 | 说明 |
|------|------|------|--------|------|
| id | BIGINT | PK, AUTO_INCREMENT | | |
| user_id | BIGINT | NOT NULL, FK | | 所属用户 |
| role_name | VARCHAR(100) | NOT NULL | | 角色名称 |
| organization | VARCHAR(200) | | NULL | 所属组织/单位 |
| start_date | DATE | NOT NULL | | 开始日期 |
| end_date | DATE | | NULL | 结束日期 |
| is_current | TINYINT(1) | | 0 | 是否在职: 0否 1是 |
| responsibilities | TEXT | | NULL | 职责描述 |
| created_at | DATETIME | | CURRENT_TIMESTAMP | |
| updated_at | DATETIME | | CURRENT_TIMESTAMP | |

---

### 3.6 ai_analysis — AI 分析记录表

| 字段 | 类型 | 约束 | 默认值 | 说明 |
|------|------|------|--------|------|
| id | BIGINT | PK, AUTO_INCREMENT | | |
| user_id | BIGINT | NOT NULL, FK | | 所属用户 |
| analysis_type | VARCHAR(50) | | NULL | 分析维度组合描述 |
| dimensions | JSON | | NULL | 雷达图维度数据 `{"学习":85,"协作":90}` |
| report_content | TEXT | | NULL | 完整报告内容(HTML/Markdown) |
| strengths | JSON | | NULL | 核心优势 `[{"title":"","desc":""}]` |
| weaknesses | JSON | | NULL | 待提升领域 `[{"title":"","desc":""}]` |
| suggestions | JSON | | NULL | 发展建议 `[{"title":"","desc":""}]` |
| competitiveness | JSON | | NULL | 竞争力评估 `{"保研":80,"就业":90}` |
| created_at | DATETIME | | CURRENT_TIMESTAMP | |

---

### 3.7 user_settings — 用户设置表

| 字段 | 类型 | 约束 | 默认值 | 说明 |
|------|------|------|--------|------|
| id | BIGINT | PK, AUTO_INCREMENT | | |
| user_id | BIGINT | NOT NULL, UNIQUE, FK | | 所属用户 |
| gpa_standard | VARCHAR(20) | | '4.0' | GPA标准: 4.0/5.0/百分制加权 |
| gpa_scale | JSON | | NULL | 绩点对照表配置 |
| ai_dimensions | JSON | | NULL | AI分析维度开关配置 |
| theme | VARCHAR(20) | | 'light' | 主题: light/dark |
| created_at | DATETIME | | CURRENT_TIMESTAMP | |
| updated_at | DATETIME | | CURRENT_TIMESTAMP | |

---

### 3.8 data_operations — 数据导入导出记录表

| 字段 | 类型 | 约束 | 默认值 | 说明 |
|------|------|------|--------|------|
| id | BIGINT | PK, AUTO_INCREMENT | | |
| user_id | BIGINT | NOT NULL, FK | | 所属用户 |
| operation_type | VARCHAR(10) | NOT NULL | | import / export |
| data_type | VARCHAR(20) | NOT NULL | | 课程/成绩/经历/成就/角色 |
| file_url | VARCHAR(255) | | NULL | 文件地址 |
| status | VARCHAR(20) | | 'pending' | 状态: pending/success/fail |
| created_at | DATETIME | | CURRENT_TIMESTAMP | |

---

## 4. 建表 SQL 语句

```sql
-- ============================================
-- 个人发展规划网站 — 数据库初始化脚本
-- MySQL 8.0+
-- ============================================

CREATE DATABASE IF NOT EXISTS `personal_dev_plan`
    DEFAULT CHARACTER SET utf8mb4
    COLLATE utf8mb4_unicode_ci;

USE `personal_dev_plan`;

-- --------------------------------------------
-- 1. 用户表 users
-- --------------------------------------------
CREATE TABLE `users` (
    `id`              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '用户ID',
    `username`        VARCHAR(50)  NOT NULL UNIQUE COMMENT '用户名',
    `email`           VARCHAR(100) NOT NULL UNIQUE COMMENT '邮箱',
    `student_id`      VARCHAR(50)  UNIQUE COMMENT '学号',
    `password`        VARCHAR(255) NOT NULL COMMENT '加密密码(BCrypt)',
    `real_name`       VARCHAR(50)  COMMENT '真实姓名',
    `avatar`          VARCHAR(255) COMMENT '头像URL',
    `major`           VARCHAR(100) COMMENT '专业',
    `grade_year`      TINYINT COMMENT '年级: 1/2/3/4',
    `created_at`      DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '注册时间',
    `updated_at`      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_email` (`email`),
    INDEX `idx_student_id` (`student_id`)
) ENGINE = InnoDB
  DEFAULT CHARSET = utf8mb4
  COLLATE = utf8mb4_unicode_ci
  COMMENT = '用户表';

-- --------------------------------------------
-- 2. 课程表 courses
-- --------------------------------------------
CREATE TABLE `courses` (
    `id`              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '课程ID',
    `user_id`         BIGINT NOT NULL COMMENT '所属用户ID',
    `course_name`     VARCHAR(200) NOT NULL COMMENT '课程名称',
    `course_type`     VARCHAR(20)  NOT NULL COMMENT '类型: 必修/选修/通识',
    `credits`         DECIMAL(3,1) NOT NULL COMMENT '学分',
    `semester`        VARCHAR(20)  NOT NULL COMMENT '学期, 如2024-2025-1',
    `academic_year`   VARCHAR(20)  COMMENT '学年, 如2024-2025',
    `score`           DECIMAL(5,1) COMMENT '成绩(0-100)',
    `grade_point`     DECIMAL(3,2) COMMENT '绩点',
    `created_at`      DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at`      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_semester` (`semester`),
    INDEX `idx_course_type` (`course_type`),
    FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE = InnoDB
  DEFAULT CHARSET = utf8mb4
  COLLATE = utf8mb4_unicode_ci
  COMMENT = '课程表';

-- --------------------------------------------
-- 3. 经历表 experiences
-- --------------------------------------------
CREATE TABLE `experiences` (
    `id`              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '经历ID',
    `user_id`         BIGINT NOT NULL COMMENT '所属用户ID',
    `title`           VARCHAR(200) NOT NULL COMMENT '经历标题',
    `type`            VARCHAR(20)  NOT NULL COMMENT '类型: 竞赛/项目/实习',
    `start_date`      DATE NOT NULL COMMENT '开始日期',
    `end_date`        DATE COMMENT '结束日期(null=至今)',
    `description`     TEXT COMMENT '详细描述',
    `result`          VARCHAR(500) COMMENT '成果/奖项',
    `attachments`     JSON COMMENT '证明材料JSON数组',
    `created_at`      DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at`      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_type` (`type`),
    INDEX `idx_start_date` (`start_date`),
    FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE = InnoDB
  DEFAULT CHARSET = utf8mb4
  COLLATE = utf8mb4_unicode_ci
  COMMENT = '经历表';

-- --------------------------------------------
-- 4. 成就表 achievements
-- --------------------------------------------
CREATE TABLE `achievements` (
    `id`              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '成就ID',
    `user_id`         BIGINT NOT NULL COMMENT '所属用户ID',
    `name`            VARCHAR(200) NOT NULL COMMENT '成就名称',
    `level`           VARCHAR(20)  NOT NULL COMMENT '级别: 国家级/省级/校级/院级',
    `category`        VARCHAR(50)  COMMENT '分类: 学术/竞赛/志愿/其他',
    `issuer`          VARCHAR(200) COMMENT '颁发机构',
    `award_date`      DATE COMMENT '获得日期',
    `description`     TEXT COMMENT '描述',
    `certificate_url` VARCHAR(255) COMMENT '证书图片URL',
    `created_at`      DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at`      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_level` (`level`),
    INDEX `idx_category` (`category`),
    INDEX `idx_award_date` (`award_date`),
    FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE = InnoDB
  DEFAULT CHARSET = utf8mb4
  COLLATE = utf8mb4_unicode_ci
  COMMENT = '成就表';

-- --------------------------------------------
-- 5. 角色表 roles
-- --------------------------------------------
CREATE TABLE `roles` (
    `id`              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '角色ID',
    `user_id`         BIGINT NOT NULL COMMENT '所属用户ID',
    `role_name`       VARCHAR(100) NOT NULL COMMENT '角色名称',
    `organization`    VARCHAR(200) COMMENT '所属组织/单位',
    `start_date`      DATE NOT NULL COMMENT '开始日期',
    `end_date`        DATE COMMENT '结束日期',
    `is_current`      TINYINT(1) DEFAULT 0 COMMENT '是否在职: 0否 1是',
    `responsibilities` TEXT COMMENT '职责描述',
    `created_at`      DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at`      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_is_current` (`is_current`),
    INDEX `idx_start_date` (`start_date`),
    FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE = InnoDB
  DEFAULT CHARSET = utf8mb4
  COLLATE = utf8mb4_unicode_ci
  COMMENT = '角色表';

-- --------------------------------------------
-- 6. AI 分析记录表 ai_analysis
-- --------------------------------------------
CREATE TABLE `ai_analysis` (
    `id`              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '分析记录ID',
    `user_id`         BIGINT NOT NULL COMMENT '所属用户ID',
    `analysis_type`   VARCHAR(50) COMMENT '分析维度组合描述',
    `dimensions`      JSON COMMENT '雷达图维度数据',
    `report_content`  TEXT COMMENT '完整报告内容',
    `strengths`       JSON COMMENT '核心优势JSON数组',
    `weaknesses`      JSON COMMENT '待提升领域JSON数组',
    `suggestions`     JSON COMMENT '发展建议JSON数组',
    `competitiveness` JSON COMMENT '竞争力评估JSON',
    `created_at`      DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_created_at` (`created_at`),
    FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE = InnoDB
  DEFAULT CHARSET = utf8mb4
  COLLATE = utf8mb4_unicode_ci
  COMMENT = 'AI分析记录表';

-- --------------------------------------------
-- 7. 用户设置表 user_settings
-- --------------------------------------------
CREATE TABLE `user_settings` (
    `id`              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '设置ID',
    `user_id`         BIGINT NOT NULL UNIQUE COMMENT '所属用户ID',
    `gpa_standard`    VARCHAR(20) DEFAULT '4.0' COMMENT 'GPA标准: 4.0/5.0/百分制加权',
    `gpa_scale`       JSON COMMENT '绩点对照表配置',
    `ai_dimensions`   JSON COMMENT 'AI分析维度开关配置',
    `theme`           VARCHAR(20) DEFAULT 'light' COMMENT '主题: light/dark',
    `created_at`      DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    `updated_at`      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_user_id` (`user_id`),
    FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE = InnoDB
  DEFAULT CHARSET = utf8mb4
  COLLATE = utf8mb4_unicode_ci
  COMMENT = '用户设置表';

-- --------------------------------------------
-- 8. 数据导入导出记录表 data_operations
-- --------------------------------------------
CREATE TABLE `data_operations` (
    `id`              BIGINT AUTO_INCREMENT PRIMARY KEY COMMENT '记录ID',
    `user_id`         BIGINT NOT NULL COMMENT '所属用户ID',
    `operation_type`  VARCHAR(10) NOT NULL COMMENT '操作类型: import/export',
    `data_type`       VARCHAR(20) NOT NULL COMMENT '数据类型: 课程/成绩/经历/成就/角色',
    `file_url`        VARCHAR(255) COMMENT '文件地址',
    `status`          VARCHAR(20) DEFAULT 'pending' COMMENT '状态: pending/success/fail',
    `created_at`      DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    INDEX `idx_user_id` (`user_id`),
    INDEX `idx_operation_type` (`operation_type`),
    INDEX `idx_status` (`status`),
    FOREIGN KEY (`user_id`) REFERENCES `users` (`id`) ON DELETE CASCADE
) ENGINE = InnoDB
  DEFAULT CHARSET = utf8mb4
  COLLATE = utf8mb4_unicode_ci
  COMMENT = '数据导入导出记录表';
```

---

## 5. 索引说明

| 表名 | 索引名 | 字段 | 用途 |
|------|--------|------|------|
| users | idx_email | email | 登录查询 |
| users | idx_student_id | student_id | 学号查询 |
| courses | idx_user_id | user_id | 用户课程列表 |
| courses | idx_semester | semester | 按学期筛选 |
| courses | idx_course_type | course_type | 按类型筛选 |
| experiences | idx_user_id | user_id | 用户经历列表 |
| experiences | idx_type | type | 按类型筛选 |
| achievements | idx_user_id | user_id | 用户成就列表 |
| achievements | idx_level | level | 按级别统计 |
| achievements | idx_category | category | 按分类筛选 |
| roles | idx_user_id | user_id | 用户角色列表 |
| roles | idx_is_current | is_current | 筛选在职角色 |
| ai_analysis | idx_user_id | user_id | 用户分析历史 |
| ai_analysis | idx_created_at | created_at | 按时间排序 |
| user_settings | idx_user_id | user_id | 用户设置查询 |
| data_operations | idx_user_id | user_id | 用户操作记录 |
| data_operations | idx_operation_type | operation_type | 按类型筛选 |
| data_operations | idx_status | status | 按状态筛选 |

---

## 附录：JSON 字段示例

### gpa_scale (绩点对照表)

```json
{
  "standard": "4.0",
  "scales": [
    {"min": 90, "max": 100, "point": 4.0},
    {"min": 85, "max": 89,  "point": 3.7},
    {"min": 82, "max": 84,  "point": 3.3},
    {"min": 78, "max": 81,  "point": 3.0},
    {"min": 75, "max": 77,  "point": 2.7},
    {"min": 72, "max": 74,  "point": 2.3},
    {"min": 68, "max": 71,  "point": 2.0},
    {"min": 64, "max": 67,  "point": 1.5},
    {"min": 60, "max": 63,  "point": 1.0},
    {"min": 0,  "max": 59,  "point": 0.0}
  ]
}
```

### ai_dimensions (AI 分析维度配置)

```json
{
  "ability_assessment": true,
  "development_direction": true,
  "resume_optimization": false,
  "competitiveness_analysis": true
}
```

### attachments (经历证明材料)

```json
[
  {"name": "获奖证书.pdf", "url": "/uploads/xxx.pdf"},
  {"name": "项目截图.png", "url": "/uploads/yyy.png"}
]
```
